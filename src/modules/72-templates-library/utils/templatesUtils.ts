export enum TemplateType {
  pipeline = 'pipeline',
  stage = 'stage',
  service = 'service',
  infrastructure = 'infrastructure',
  step = 'step',
  stepGroup = 'stepGroup',
  execution = 'execution'
}
