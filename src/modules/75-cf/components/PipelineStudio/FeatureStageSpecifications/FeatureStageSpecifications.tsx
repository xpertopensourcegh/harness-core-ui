import React from 'react'
import { Layout } from '@wings-software/uicore'

export default function FeatureStageSpecifications(_props: React.PropsWithChildren<unknown>): JSX.Element {
  /*const {
    state: {
      pipeline,
      selectionState: { selectedStageId = '' }
    },
    updatePipeline,
    getStageFromPipeline
  } = React.useContext(PipelineContext)*/

  return <Layout.Vertical spacing="large">TODO</Layout.Vertical>
}
