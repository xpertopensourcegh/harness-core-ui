[
  {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Person",
    "type": "object",
    "properties": {
      "firstName": {
        "type": "string",
        "description": "The person's first name."
      },
      "lastName": {
        "type": "string",
        "description": "The person's last name."
      },
      "age": {
        "description": "Age in years which must be equal to or greater than zero.",
        "type": "integer",
        "minimum": 0
      }
    }
  },
  {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Animal",
    "type": "object",
    "properties": {
      "species": {
        "type": "string",
        "description": "The animals's species."
      },
      "height": {
        "type": "string",
        "description": "The animals's height."
      },
      "age": {
        "description": "Age in years which must be equal to or greater than zero.",
        "type": "integer",
        "minimum": 0
      }
    }
  }
]
