//TODO Remove dummy data when API becomes available
export function fetchExpressions() {
  return [
    'pipeline.stage',
    'pipeline.env',
    'pipeline.env.name',
    'stage.type',
    'stage.name',
    'stage.env',
    'stage.env.name',
    'inputSet.name',
    'inputSet.type'
  ]
}
